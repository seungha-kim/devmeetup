function async(type) {
  return `${type}_ASYNC`
}

export const STEP_ON = 'STEP_ON'
export const STEP_OFF = 'STEP_OFF'

export const COUNTER_INCREMENT = 'COUNTER_INCREMENT'
export const COUNTER_INCREMENT_ASYNC = async(COUNTER_INCREMENT)
